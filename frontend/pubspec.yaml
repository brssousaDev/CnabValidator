name: cnab_validador
description: "CNAB Validador - Validation, visualization and correction of CNAB files"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  http: ^1.1.0
  file_picker: ^5.2.0
  intl: ^0.19.0
  universal_html: ^2.2.0
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
